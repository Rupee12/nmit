import {useEffect,useState} from 'react';import {motion,AnimatePresence} from 'framer-motion';import gsap from 'gsap';import {appreciationMessages} from '../data/messages';
export default function Appreciation(){const [i,setI]=useState(0);useEffect(()=>{const id=setInterval(()=>setI(x=>(x+1)%appreciationMessages.length),4500);return()=>clearInterval(id)},[]);return <section className="px-6 py-32 border-y border-white/5"><div className="max-w-5xl mx-auto text-center"><p className="text-xs tracking-[.3em] uppercase text-[#d9b76c]">A note of gratitude</p><h2 className="serif text-6xl md:text-8xl mt-5">Because every journey has a mentor.</h2><div className="min-h-[180px] flex items-center justify-center mt-12"><AnimatePresence mode="wait"><motion.p key={i} initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-18}} className="serif italic text-3xl md:text-5xl text-white/75 max-w-4xl">“{appreciationMessages[i]}”</motion.p></AnimatePresence></div><div className="flex justify-center gap-2 mt-8">{appreciationMessages.map((_,x)=><button aria-label={`Message ${x+1}`} key={x} onClick={()=>setI(x)} className={`w-1.5 h-1.5 rounded-full ${x===i?'bg-[#d9b76c]':'bg-white/20'}`}/>)}</div></div></section>}
