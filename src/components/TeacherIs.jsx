import {useEffect,useRef} from 'react';import gsap from 'gsap';import {ScrollTrigger} from 'gsap/ScrollTrigger';gsap.registerPlugin(ScrollTrigger);
export default function TeacherIs(){const ref=useRef();useEffect(()=>{const ctx=gsap.context(()=>{gsap.from('.teacher-word',{scrollTrigger:{trigger:ref.current,start:'top 70%'},opacity:0,y:35,stagger:.18,duration:.8,ease:'power3.out'});gsap.from('.everything',{scrollTrigger:{trigger:ref.current,start:'top 35%'},opacity:0,scale:.8,duration:1.2,ease:'power3.out'})},ref);return()=>ctx.revert()},[]);return <section ref={ref} className="min-h-screen flex items-center justify-center px-6 py-32"><div className="text-center"><p className="text-xs tracking-[.3em] uppercase text-[#d9b76c]">If we had to put it into words</p><h2 className="serif text-7xl md:text-9xl mt-8">A teacher is...</h2><div className="mt-14 flex flex-wrap justify-center gap-x-7 gap-y-3 text-2xl md:text-4xl text-white/65">{['Mentor.','Guide.','Inspiration.','Support.','Motivation.'].map(w=><span className="teacher-word" key={w}>{w}</span>)}</div><p className="mt-16 text-white/35 italic">And sometimes...</p><div className="everything serif text-[18vw] leading-none text-[#d9b76c]">Everything.</div></div></section>}
