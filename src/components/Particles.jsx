import {useEffect,useState} from 'react';
export default function Particles(){const [dots,setDots]=useState([]);useEffect(()=>{setDots(Array.from({length:38},(_,i)=>({id:i,left:Math.random()*100,top:Math.random()*100,size:1+Math.random()*2,d:7+Math.random()*12,delay:Math.random()*8})))},[]);return <div aria-hidden className="fixed inset-0 pointer-events-none overflow-hidden opacity-40">{dots.map(d=><span key={d.id} className="absolute rounded-full bg-[#d9b76c] animate-pulse" style={{left:`${d.left}%`,top:`${d.top}%`,width:d.size,height:d.size,animationDuration:`${d.d}s`,animationDelay:`${d.delay}s`}}/> )}</div>}
