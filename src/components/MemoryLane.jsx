import {useState} from 'react';import {gallery} from '../data/messages';import {X} from 'lucide-react';
export default function MemoryLane(){const [selected,setSelected]=useState(null);return <section className="px-6 py-32 bg-white/[.015]"><div className="max-w-6xl mx-auto"><p className="text-xs tracking-[.3em] uppercase text-[#d9b76c]">Memory lane</p><h2 className="serif text-6xl md:text-8xl mt-4">Moments we've shared.</h2><div className="grid md:grid-cols-4 gap-4 mt-14">{gallery.map((g,i)=><button key={g.src} onClick={()=>setSelected(g)} className={`focus-ring overflow-hidden rounded-2xl text-left ${i%3===0?'md:row-span-2':''}`}><img loading="lazy" src={g.src} alt={g.caption} className="w-full h-full min-h-56 object-cover opacity-75 hover:opacity-100 hover:scale-105 transition duration-700"/><span className="sr-only">{g.caption}</span></button>)}</div></div>{selected&&<div role="dialog" aria-modal="true" className="fixed inset-0 z-[60] bg-black/90 backdrop-blur-xl p-6 flex items-center justify-center" onClick={()=>setSelected(null)}><button className="absolute top-6 right-6 focus-ring" aria-label="Close"><X/></button><img src={selected.src} alt={selected.caption} className="max-h-[85vh] max-w-5xl rounded-2xl"/></div>}</section>}
